---
import ls from "../../friends.json";
import FriendCard from "./FriendCard.astro";
---

<section
  class="container xl:max-w-[980px] mx-auto grid md:grid-cols-3 p-30 gap-10 sm:grid-cols-2 grid-cols-1 pt-30"
>
  {ls.map((i) => <FriendCard info={i} />)}
</section>
